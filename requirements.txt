# ── Core framework ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Cache ─────────────────────────────────────────────────────────────────────
redis>=5.0.0

# ── TTS providers (install the ones you actually use) ─────────────────────────

# Google Cloud TTS
google-cloud-texttospeech>=2.16.0

# AWS Polly
boto3>=1.34.0

# OpenAI TTS
openai>=1.30.0

# ElevenLabs
requests>=2.31.0   # used by ElevenLabs backend (already a transitive dep)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-multipart>=0.0.9   # required for FastAPI Form() / file uploads
jinja2>=3.1.0   # required for FastAPI Jinja2Templates
openpyxl>=3.1.0   # XLSX export for admin reports

# ── Audio Conversion ─────────────────────────────────────────────────────────
# ffmpeg is required for audio format conversion (MP3 to WAV, etc.)
# Install ffmpeg on your system:
#   Ubuntu/Debian: sudo apt-get install ffmpeg
#   CentOS/RHEL: sudo yum install ffmpeg
#   macOS: brew install ffmpeg
#   Windows: Download from https://ffmpeg.org/download.html
# The application will work without ffmpeg but will skip audio conversion.

# ── Direct SIP UA / PJSIP ─────────────────────────────────────────────────────
# Install the PJSUA2 Python bindings provided by your deployment/runtime.
# Package name and installation method vary by platform; this project loads
# the `pjsua2` module lazily at runtime and degrades safely when unavailable.
